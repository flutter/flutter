name: hello_world
version: 1.0.0+1

environment:
  sdk: ^3.9.0-0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

flutter:
  assets:
    - assets/data/styrketraening_oevelser.json


dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  test: any

  webdriver: any

# PUBSPEC CHECKSUM: mut18q
