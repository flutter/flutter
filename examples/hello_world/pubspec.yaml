name: hello_world
description: A minimal Flutter application.

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  application:
    androidPackage: com.example.helloworld
    iosBundleIdentifier: com.example.helloWorld
