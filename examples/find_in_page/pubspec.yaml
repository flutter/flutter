name: find_in_page
description: Interactive showcase for app-level Find-in-Page (Cmd+F / Ctrl+F) using SelectionArea and FindInPageController (flutter/flutter#65504).
publish_to: none

environment:
  sdk: ^3.11.0-0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
