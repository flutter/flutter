name: seo_example
description: Example demonstrating Flutter Web SEO capabilities.

environment:
  sdk: ^3.9.0-0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

# PUBSPEC CHECKSUM: t3blht