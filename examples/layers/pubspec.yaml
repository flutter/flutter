name: flutter_examples_layers

environment:
  sdk: ^3.11.0-0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - services/data.json
  uses-material-design: true

# PUBSPEC CHECKSUM: 60tfp7
