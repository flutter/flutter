# Tests configuration file.
#
# It is used to define the tests that will be run on this repository
# it uses a very simple schema:
#
# tasks:
#   - task: <folder name>
#     script: <relative path to the runner script>

tasks:
  - task: ci
    script: deps_parser_tests.py
