# Image Filter Tests

This directory contains unit tests for the `ImageFilter` class.

## Files

- **`image_filter_test.dart`**: Contains tests for the `ImageFilter` class, specifically focusing on its constructors (`matrix` and `blur`). It verifies that matrices are correctly copied upon filter creation and that equality checks accurately account for all filter parameters, such as sigma and tile mode.
