name: web_sdk_tests

# Keep the SDK version range in sync with lib/web_ui/pubspec.yaml
environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  args: 2.3.1
  path: 1.8.2

dev_dependencies:
  analyzer: 4.3.1
  test: 1.22.1
