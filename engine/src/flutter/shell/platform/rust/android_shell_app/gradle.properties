org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
# Matches default_android_sdk_version in //build/config/android/config.gni;
# AGP 8.5.2 hasn't been tested against it yet but builds cleanly.
android.suppressUnsupportedCompileSdk=36
