// Android milestone 1 scaffold (see flutter-rs-proggress.md): a hand-maintained
// Gradle project that packages the flutter-shell-android-runner cdylib and
// launches it through GameActivity. This is not yet generated by the Flutter
// tool; that comes with a later Phase 4 milestone.
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "flutter_rust_shell_android"
include(":app")
