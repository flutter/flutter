# ios_universal_link_appdelegate

This is an integration test designed to verify that iOS Universal Links and custom scheme deep links correctly work with the classic `UIApplicationDelegate` lifecycle (without `UIScene`), ensuring routes and plugin events are accurately forwarded.

This test is executed by the Devicelab task `ios_universal_link_test.dart`.
