# font_assets

Adding and treeshaking fonts through the hooks.

Uncomment the `fonts:` lines in the pubspec and rename the `build.dart` hook to return to the usual behavior.


TODO: Currently requires two runs of `flutter build linux`, the first one errors. No idea why at the moment.