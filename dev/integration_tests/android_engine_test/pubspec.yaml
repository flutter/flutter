name: android_engine_test
publish_to: none

environment:
  sdk: ^3.8.0-0

resolution: workspace

dependencies:
  android_driver_extensions:
    path: ../../tools/android_driver_extensions
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  flutter_gpu:
    path: ../../../engine/src/flutter/lib/gpu

  matcher: any
  path: any
  vector_math: any

dev_dependencies:
  test: any

  args: any

flutter:
  assets:
    - test.shaderbundle

# PUBSPEC CHECKSUM: 2fd5ap
