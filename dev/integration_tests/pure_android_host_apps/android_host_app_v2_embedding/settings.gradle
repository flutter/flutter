// Copyright 2014 The Flutter Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        def flutterStorageUrl = System.getenv("FLUTTER_STORAGE_BASE_URL") ?: "https://storage.googleapis.com"
        maven {
            url = uri("$flutterStorageUrl/download.flutter.io")
        }
        maven {
            url = uri("../hello/build/host/outputs/repo")
        }
        // BEGIN CI only configuration (for pre-submits which contain engine changes).
        // END CI only configuration (for pre-submits which contain engine changes).
    }
}

include ':app'
