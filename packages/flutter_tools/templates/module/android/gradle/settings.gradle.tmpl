rootProject.name = '{{projectName}}'

plugins {
    id "com.android.library" version "{{gradleVersionForModule}}" apply false
    id "org.jetbrains.kotlin.android" version "{{kotlin_version}}" apply false
}