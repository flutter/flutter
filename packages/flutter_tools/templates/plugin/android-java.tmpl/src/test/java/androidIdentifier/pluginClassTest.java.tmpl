package {{androidIdentifier}};

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/**
 * This demonstrates a simple unit test of the Java portion of this plugin's implementation.
 *
 * Once you have built the plugin's example app, you can run these tests from the command
 * line by running `./gradlew testDebugUnitTest` in the `example/android/` directory, or
 * you can run them directly from IDEs that support JUnit such as Android Studio.
 */

public class {{pluginClass}}Test {
  @Test
  public void getPlatformVersion_returnsExpectedValue() {
    {{pluginClass}} plugin = new {{pluginClass}}();

    String result = plugin.getPlatformVersion();

    assertEquals("Android " + android.os.Build.VERSION.RELEASE, result);
  }
}
