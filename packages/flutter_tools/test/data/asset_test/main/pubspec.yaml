name: main
description: A test project that has a package with a font as a dependency.

environment:
  sdk: '>=3.2.0-0 <4.0.0'

dependencies:
  font:
    path: ../font

flutter:
  assets:
    - dummy-cert.pem
    - dummy-key.pem
# PUBSPEC CHECKSUM: 0000
