name: main
description: A test project that has a package with a font and dummy certificates as a dependencies.

environment:
  sdk: '>=3.0.0-0 <4.0.0'

dependencies:
  font:
    path: ../font
  tls_cert:
    path: ../tls_cert

# PUBSPEC CHECKSUM: 0000
