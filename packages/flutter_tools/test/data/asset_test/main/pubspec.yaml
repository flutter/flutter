name: main
description: A test project that has font and dummy certificates packages as dependencies.

environment:
  sdk: '>=3.0.0-0 <4.0.0'

dependencies:
  font:
    path: ../font
  tls_cert:
    path: ../tls_cert

# PUBSPEC CHECKSUM: 0000
