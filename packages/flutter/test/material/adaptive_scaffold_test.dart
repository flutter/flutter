import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('adaptive scaffold lays out navigation as expected', (WidgetTester tester) async {});
  testWidgets('adaptive scaffold properly manages switches in body and secondary body', (WidgetTester tester) async {});
  testWidgets('adaptive scaffold animations work correctly', (WidgetTester tester) async {});
  testWidgets('adaptive scaffold animations can be disabled', (WidgetTester tester) async {});
}
