# iOS Agent

## When to use
Use this agent when developing iOS platform features in Flutter, such as the engine's iOS embedder or iOS command-line tools.

## How to use
Select "ios-agent" from the agent menu. For example, when using Antigravity, the agent dropdown menu is located in the chat box when starting a new thread.
